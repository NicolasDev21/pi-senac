<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <link rel="stylesheet" href="/css/login.css">
    <script src="/scripts/script_login.js" type="text/javascript" defer></script>

    <title>Login</title>
</head>

<body>
    {{#if userAuthLogged}}
        <span id="userAuthLogged">true</span>
    {{/if}}

    {{#if userNotIdentify}}
        <p id="userNotIdentify">true</p>
    {{/if}}

    {{#if checkCredentials}}
        <p id="checkCredentials">true</p>
    {{/if}}

    <main>

        <section id="section-text">
            <h1>Seja bem vindo(a) ao...</h1>
            <h2>Mãos Mágicas Massagem !</h2>
            <h3>Faça login e agende sua consulta conosco.</h3>

            <article id="container-button-float">
                <div class="div-button-float">
                    {{!-- Icon home --}}
                    <a href="/"><i class="fi fi-sr-home"></i></a>
                </div>

                <div class="div-button-float">
                    {{!-- Icon cadaster user --}}
                    <a href="/cadaster"><i class="fi fi-rr-user-add"></i></a>
                </div>
            </article>
        </section>

        <section id="section-form">

            <header id="header-form">
                <i id="btn-user" class="fi fi-rr-circle-user"></i>

                <h2>Login</h2>
            </header>

            <form action="/login" name="form-login" method="POST">

                <p id="text-info-login">* Acesse sua conta por aqui! *</p>


                <article id="field-username" class="field-class-input">
                    <i class="fi fi-rr-user"></i>

                    <input id="input-username" type="text" name="username" placeholder="Digite seu nome de usuário">
                </article>


                <article id="field-password" class="field-class-input">
                    <i class="fi fi-ss-key"></i>

                    <input id="input-pass" type="password" name="password" placeholder="Digite sua senha de login">

                    <i id="icon-eye-pass" class="fi fi-ss-low-vision"></i>
                </article>

                <article id="container-interaction">
                    <a href="/login">I forgot my password!</a>

                    <div class="field-buttons">
                        <input class="btn" type="reset" value="Cancelar">
                        <input id="btn-submit" class="btn" type="submit" value="Logar">
                    </div>
                </article>
            </form>
        </section>

    </main>

</body>

</html>